/**
 * @file
 * Behaviors for the Adimeo install theme.
 */

(function ($, _, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.AdimeoInstrall = {
    attach: function (context) {
      // Adimeo install JavaScript behaviors goes here.
    }
  };

})(window.jQuery, window._, window.Drupal, window.drupalSettings);
